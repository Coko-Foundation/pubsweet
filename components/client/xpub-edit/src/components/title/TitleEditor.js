import React from 'react'
import ConfigurableEditor from '../configurable/ConfigurableEditor'

const TitleEditor = props => (
  <ConfigurableEditor italic smallcaps subscript superscript {...props} />
)

export default TitleEditor
