image: node:8

cache:
  paths:
    - node_modules/

before_script:
  - yarn

test:
  script:
    - npm run test
  coverage: '/^All files\s+\|\s+(\d+.\d+)\s\|.*$/'

test:vulnerabilities:
  script:
    - npm run vuln-test

