// This is an unexpored helper/utiltiy function, that directly
// boots up the server (usually this happens through pubsweet-cli,
// but this way can be useful in some environments)

const startServer = require('./start-server')

startServer()
